let numeroParaTabla = 2;
let multiplicador = 1; 
while (multiplicador <= 10) { 
  let resultado = numeroParaTabla * multiplicador;
  console.log(`${numeroParaTabla} x ${multiplicador} = ${resultado}`);
  multiplicador++; 
}
